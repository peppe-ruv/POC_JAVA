{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "heroku-postbuild": "cd ../frontend/mini-web-app && npm install && npm run build -- --configuration production && mkdir -p ../../backend/public && cp -r dist/mini-web-app/browser/* ../../backend/public/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "pg": "^8.16.0"
  },
  "engines": {
    "node": "18.x"
  }
}
